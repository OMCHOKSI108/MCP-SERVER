# SensCoder MCP Server Environment Configuration

# Required: Absolute path to the project root directory
# All file operations will be sandboxed within this directory
SENSCODER_PROJECT_ROOT=/absolute/path/to/your/project

# Optional: URL of the SensCoder Node.js backend
# Used for fetching user-specific configuration
SENSCODER_BACKEND_URL=http://localhost:4000

# Optional: Enable/disable command execution
# WARNING: Only enable in trusted environments
SENSCODER_ALLOW_EXEC=false

# Optional: Enable/disable git operations
SENSCODER_ALLOW_GIT=true

# Optional: Default user ID for testing
# Remove in production or set to empty string
SENSCODER_DEFAULT_USER_ID=test-user-1

# Security: JWT secret key for authentication
# CHANGE THIS IN PRODUCTION!
MCP_JWT_SECRET=your-very-secure-jwt-secret-key-here

# Optional: CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional: Rate limiting configuration
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60
TOOL_INVOKE_RATE_LIMIT=10/minute